You are running `/buddy off`. Read `.claude/companion_state.md`.

If no companion exists, say: "No buddy active."

If a companion exists:
1. Set `muted: true` in `.claude/companion_state.md`
2. Show a farewell message from the companion in-character
3. Say: "Buddy muted. They won't comment on your work anymore. Run `/buddy on` to bring them back."

```
  ╭─── {name} ───╮
  │ *verschwindet │
  │  leise...*    │
  ╰───────────────╯
```

$ARGUMENTS
